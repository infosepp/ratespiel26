/**
 * Die Klasse {@code Computer} repräsentiert einen Computer-Spieler.
 * <p>
 * Der Computer soll selbstständig eine Zahl raten,
 * z. B. mithilfe eines Zufallsgenerators.
 *
 * @author ---
 */
class Computer extends Spieler {

    /**
     * Erzeugt eine vom Computer geratene Zahl.
     *
     * @return die vom Computer erzeugte Zahl
     * @author Phil
     */
    @Override
    public int raten() {
        return 0;
    }
}
